/**
 * UGC Guard API
 * API for UGC Guard. A tool to help you manage reports on user generated content.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import UserOrganizationMembershipState from './UserOrganizationMembershipState';

/**
 * The UserWithMembershipState model module.
 * @module model/UserWithMembershipState
 * @version 1.0.0
 */
class UserWithMembershipState {
    /**
     * Constructs a new <code>UserWithMembershipState</code>.
     * A user model that includes the membership state in the requested organization.
     * @alias module:model/UserWithMembershipState
     * @param username {String} 
     * @param email {String} 
     * @param membershipState {module:model/UserOrganizationMembershipState} 
     */
    constructor(username, email, membershipState) { 
        
        UserWithMembershipState.initialize(this, username, email, membershipState);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, username, email, membershipState) { 
        obj['username'] = username;
        obj['email'] = email;
        obj['membership_state'] = membershipState;
    }

    /**
     * Constructs a <code>UserWithMembershipState</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UserWithMembershipState} obj Optional instance to populate.
     * @return {module:model/UserWithMembershipState} The populated <code>UserWithMembershipState</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UserWithMembershipState();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('username')) {
                obj['username'] = ApiClient.convertToType(data['username'], 'String');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('avatar_url')) {
                obj['avatar_url'] = ApiClient.convertToType(data['avatar_url'], 'String');
            }
            if (data.hasOwnProperty('membership_state')) {
                obj['membership_state'] = UserOrganizationMembershipState.constructFromObject(data['membership_state']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UserWithMembershipState</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UserWithMembershipState</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UserWithMembershipState.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['username'] && !(typeof data['username'] === 'string' || data['username'] instanceof String)) {
            throw new Error("Expected the field `username` to be a primitive type in the JSON string but got " + data['username']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['avatar_url'] && !(typeof data['avatar_url'] === 'string' || data['avatar_url'] instanceof String)) {
            throw new Error("Expected the field `avatar_url` to be a primitive type in the JSON string but got " + data['avatar_url']);
        }

        return true;
    }


}

UserWithMembershipState.RequiredProperties = ["username", "email", "membership_state"];

/**
 * @member {String} id
 */
UserWithMembershipState.prototype['id'] = undefined;

/**
 * @member {String} username
 */
UserWithMembershipState.prototype['username'] = undefined;

/**
 * @member {String} email
 */
UserWithMembershipState.prototype['email'] = undefined;

/**
 * @member {String} avatar_url
 */
UserWithMembershipState.prototype['avatar_url'] = undefined;

/**
 * @member {module:model/UserOrganizationMembershipState} membership_state
 */
UserWithMembershipState.prototype['membership_state'] = undefined;






export default UserWithMembershipState;

